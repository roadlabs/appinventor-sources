# offline-tauri

Added Tauri 2 desktop wrapper for the App Inventor offline frontend. This directory is not part of the original upstream App Inventor source tree.

Build from repository root:

```bash
./offline-tauri/build-webapp.sh
cd offline-tauri/src-tauri
cargo tauri build
```
